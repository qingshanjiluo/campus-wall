[project]
name = "campus-wall-api"
version = "0.1.0"
description = "CampusWall Cloudflare Python Workers API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]